#!/bin/bash
function realpath { echo $(cd $(dirname $1); pwd)/$(basename $1); }
SCRIPT=`realpath $0`
ROOT_PATH="$(dirname $SCRIPT)/../"

echo "Server setup..."
cd "$ROOT_PATH/server" && bin/setup

wait

echo "Client setup..."
cd "$ROOT_PATH/client" && npm install

# Add additional steps
